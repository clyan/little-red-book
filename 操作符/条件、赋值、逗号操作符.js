
const { log } = console;
log('===================逗号操作===================');
// 最后一个进行赋值
let foo = (1,2,3,4,5)  // 5
log(foo)

log('===================条件操作===================');
let num1 = 1, num2 = 4;
let  value = (num1 > num2) ? num1 : num2;
log(value) // 4

log('===================赋值操作===================');
// 不会提升性能，只是简写语法
/**
 * =
 * *=
 * -=
 * +=
 * /=
 * <<=
 * >>=
 * >>>=
 * %=
 */